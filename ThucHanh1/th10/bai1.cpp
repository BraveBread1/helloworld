#include<iostream>
#include<fstream>

using namespace std;

int strcom(const char a[], const char b[]) {
    for(auto *i = a; i < a + n; ++i) {

    }
}

int main() {
    string s1; cin >> s1;
    string s2; cin >> s2;
    cout << strcom(s1, s2);
}