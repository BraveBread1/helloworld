#include<iostream>
#include<math.h>

using namespace std;

int main () {
    int a, b;
    cout << "Nhap vao a va b:" << endl;
    cin >> a >> b;
    long long tich = (long long)a * b;
    cout << "Tich hai so vua nhap la: " << tich;
    return 0;
}