#include<iostream>

using namespace std;

int main() {
    int t; cin >> t;
    for(int i = 1; i <= t; ++i) {
        int n; cin >> n;
        int a[n + 1];
        for(int j = 1; j <= n; ++j) {
            cin >> a[i];
        }
    }
}